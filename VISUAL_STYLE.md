# B-roll 视觉风格偏好

本文记录已确认的个人视觉偏好，供后续设计和修改 B-roll 场景时参考。

本文只描述风格原则，不保存具体场景方案，也不替代 Project / Scene JSON。

## 整体感觉

- 所有视频均为 faceless，不使用真人出镜或 Talking Head；口播由 B-roll 动画、产品录屏和有意留白共同承载。
- 接近简洁、精致的 Keynote 产品演示，而不是传统信息卡片式 PPT。
- 使用视觉对象和演示过程讲故事，不依赖大量说明文字。
- 一个场景只表达一个清楚的主题，内容变化时应拆分场景。
- 保持明显的视觉焦点、充足留白和清晰阅读顺序。
- 画面首先要整齐、稳定、易读，不为了活泼而制造凌乱感。

## 布局

- 元素优先使用规整的行列、统一尺寸、统一间距和明确对齐。
- Logo、术语或同类对象应形成整齐的网格或序列。
- 默认不旋转元素，不使用歪斜、散落、交错堆叠的构图。
- 避免对象互相覆盖，也避免在一个画面中同时塞入过多概念。
- 不机械重复“标题 + 多张圆角卡片”的通用模板。
- 卡片只用于真正的容器；流程、循环、分层和组装应使用对应的视觉结构表达。

## Logo 与图片

- Logo 本身是主要视觉对象，下方只保留清晰的名称文字。
- Logo 图片和名称文字应作为一个 Group 整体移动。
- 已有素材使用独立 Image 图层，不把 Logo 画进形状或文字中。
- 适合使用品牌 Logo、产品图标或应用图标时，即使素材尚未提供，也使用 `src: null` 的 Image 占位框，并保留必要的独立名称文字。
- Logo 和产品/应用图标的占位框默认使用圆角正方形；截图占位框则保持对应界面所需的实际比例。
- 画面需要产品界面、网页、终端状态或其他真实截图时，使用 `src: null` 的 Image 占位框等待用户手动替换，不绘制仿制截图。
- 不使用“形状 + 字母缩写”伪造临时 Logo，避免后续难以直接替换。
- 占位框的图层名称应说明所需素材，但画面中不得出现“待替换”“截图占位”等制作备注。
- 替换图片时应保持位置、尺寸、变换和动画不变。

## 文字与术语

- 面向中文观众时以中文为主要画面语言，仅产品名、命令、代码和习惯保留英文的技术名词使用英文。
- 不为了视觉风格添加文稿中没有出现的能力名称、分类标签、英文口号或补充结论。
- 文字字号要大，缩小预览时仍能快速识别。
- 文字主要负责命名、关键词和最终结论，不重复整段口播。
- 术语应使用整齐、统一的布局，不使用歪斜胶囊或随机散布。
- 不用连续空格把多个对象的标签硬排在同一个 Text 图层中；每个对象使用独立文字层。
- 不默认给文字添加底板；对齐清楚、间距规整的文字可以直接展示，以保持简洁和留白。
- 文字底板只在确实承担分组、容器或对比语义时使用，不把每个标签都做成卡片。

## 边线与容器

- 默认不为图形、卡片或文字框添加边线，避免重复勾勒轮廓造成啰嗦感。
- 只有半透明图形或半透明文字框需要强化边界时，才使用克制的细边线。
- 终端、文件夹等对象优先依靠填充色、层级和留白建立轮廓，不再额外描边。

## 色彩

- 透明背景和黑色预览底板上，以白色、灰色为基础。
- 品牌色优先来自真实 Logo。
- 橙色和黄色只用于强调重点、行动或结论，不让所有元素同时争夺注意力。
- 同一个概念在不同场景中应尽量保持相同颜色和视觉身份。

## 动画

- 动画跟随口播逐个引入对象，让画面随讲解逐步建立。
- 同类元素使用一致、克制的动画方向，优先淡入或垂直进入。
- 不使用 Scale 动效。
- 不使用多个方向乱飞、频繁旋转或无意义弹跳。
- Logo、底板和名称作为一个 Group 整体动画，不分别零碎运动。
- 动画用于解释组装、流动、循环、分支、安装和包含关系，而不只是装饰性入场。
- 不需要默认让所有对象退场；只有转场语义需要时才添加 Exit。

## 场景组织

- Agent 工具、Agent 术语等不同语义应拆成独立场景，不在同一页混杂。
- 每个场景保持一个明确视觉目的，例如 Logo 展示、结构组装、信息流或流程演示。
- 修改局部场景时只改目标场景，不重新生成无关场景。
- 保留既有时间锚点；需要拆分或调整边界时先确认场景方案。

## 避免事项

- 歪歪扭扭的旋转布局。
- 随机散落、重叠和过度拥挤。
- 用形状内文字冒充可替换图片。
- 大量相似圆角卡片反复套用。
- 小字号、长段文字和字幕照抄。
- 与口播无关的装饰动画。
- 可见的占位说明、制作备注或虚构素材名称。
