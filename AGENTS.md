# AI-Broll-Assistant

仅供个人使用的 YouTube B-roll 动画制作工具。

本文件定义项目的固定边界、技术架构、领域模型、目录约定与开发原则，是所有后续工作（包括 OpenCode / Pi、AI 辅助编码和人类开发）的唯一约束来源。未经明确确认，不得偏离本文件确立的规则。

---

## 1. 项目定位

- 项目名称：AI-Broll-Assistant
- 用途：个人使用的 YouTube B-roll 动画制作工具
- 不是通用视频编辑器，不追求 NLE（Non-Linear Editor）能力
- 不面向多用户，不做产品化封装

不在范围内：多用户、账号系统、云端数据库、多人协作、移动端、云端渲染、音频编辑、原始视频剪辑、人像 / 录屏轨道、通用视频编辑器能力、专业关键帧曲线系统。

---

## 2. 核心工作流

```
SRT 文稿
  → AI 拆分场景
  → AI 生成初始画面
  → Fabric.js 手动编辑
  → Remotion 预览和渲染
  → 输出透明背景视频
  → 导入 DaVinci Resolve
```

各阶段说明：

- SRT 文稿：本地字幕文件，作为场景拆分的输入。
- AI 拆分场景：由 OpenCode / Pi 读取 SRT，按语义和镜头意图切分为场景列表。
- AI 生成初始画面：由 OpenCode / Pi 根据场景语义生成结构化的场景初稿，包括文字、图片引用、图形、卡片、箭头、布局和动效配置，并写入场景 JSON。
- Fabric.js 手动编辑：人类在 React 编辑器中，基于 Fabric.js 对每个场景的文字、图片、图形等元素做位置、样式、层级的最终调整。
- Remotion 预览和渲染：Remotion Player 提供实时预览，Remotion 负责最终的视频渲染。
- 输出透明背景视频：渲染结果为带 Alpha 通道的视频，可直接在剪辑软件中叠加。
- 导入 DaVinci Resolve：作为 B-roll 素材进入既有剪辑流程，不替代剪辑软件。

---

## 3. 固定技术架构

技术栈为项目级硬性约束，不得替换或扩展为同位替代品。

- React：编辑器界面与应用状态
- TypeScript：全量类型约束
- Fabric.js：手动布局编辑（文字、图片、图形等元素）
- Remotion：动画计算、预览与最终视频渲染
- Remotion Player：编辑器内嵌预览
- 本地 Node.js 服务：本地项目文件读写、素材管理、调用渲染
- OpenCode 或 Pi：AI 对话入口，负责读取 SRT、生成场景、修改项目 JSON

未经明确确认，不得改变以上任一技术选型。

---

## 4. 各部分职责

### 4.1 React

- 编辑器界面与所有应用级 UI 状态
- 场景列表、属性面板、工具栏、预览窗口的容器
- 持有当前打开项目的内存态，发起保存与读取

### 4.2 Fabric.js

- 文字、图片、图形等元素的手动布局编辑
- 接受来自领域模型 JSON 的图层数据，通过 Fabric Adapter 渲染
- 编辑完成后将结果写回领域模型 JSON
- 仅负责"基础状态"，不直接处理时间轴动画

### 4.3 Remotion

- 基于领域模型 JSON 计算每一帧的画面
- 负责动画插值、预览与最终视频渲染
- 通过 Remotion Adapter 从领域模型 JSON 构造 Remotion 组件树
- 渲染输出透明背景视频

### 4.4 本地 Node.js 服务

- 读取和保存本地项目文件（projects/video001/...）
- 解析、保存、校验领域模型 JSON
- 提供素材访问与渲染调用
- 是 Fabric.js 编辑端与 Remotion 渲染端共享的文件与数据中介

### 4.5 OpenCode 或 Pi

- 读取本地 SRT 文稿
- 拆分场景、生成初始画面建议
- 通过本地服务读写项目 JSON
- 不直接操作内存中的编辑器状态，只操作已落盘的数据

---

## 5. 数据模型与适配器

### 5.1 唯一数据源

Project / Scene 领域模型 JSON 是项目的唯一数据源。Fabric.js、Remotion、React UI 均不持有独立于领域模型的真相。

### 5.2 数据流向

```
领域模型 JSON → Fabric Adapter → Fabric.js
领域模型 JSON → Remotion Adapter → Remotion
OpenCode / Pi → 领域模型 JSON
```

### 5.3 禁止项

- 禁止将 Fabric.js 的 `canvas.toJSON()` 直接作为持久化格式
- 禁止让 Remotion 直接读取 Fabric.js 私有数据
- 禁止将每个场景生成成一次性的 React 代码（即禁止以"代码生成"代替"数据驱动"）

Fabric 私有结构、Remotion 内部结构都只是视图层瞬时态，持久化只能是领域模型 JSON。

### 5.4 图层分组

- Scene 顶层允许普通图层与 Group 图层
- Group 的子图层使用相对于 Group 的局部坐标
- Group 自身可移动、旋转、等比缩放、控制透明度、可见性、锁定并配置动画
- Group 子图层保留各自样式与动画，Remotion 按“Group 动画 → 子图层动画”的层级组合
- 第一版不支持嵌套 Group、非等比缩放、裁剪、Fill、Stroke 与圆角；需要视觉容器时应另行讨论 Frame 概念
- Group 必须保存在领域模型 JSON 中，禁止以 Fabric.js 的 Group 私有数据作为持久化格式

---

## 6. 项目目录约定

```
projects/
└── video001/
    ├── source.srt
    ├── project.json
    ├── scenes/
    │   ├── scene-001.json
    │   └── scene-002.json
    ├── assets/
    └── renders/
```

约定：

- `source.srt`：本项目的原始字幕输入
- `project.json`：项目配置 + 场景引用列表，不保存每个场景的完整图层数据
- `scenes/*.json`：每个场景的完整图层与动画数据
- `assets/`：本项目用到的图片、字体等素材
- `renders/`：Remotion 输出的视频文件

禁止在两个位置重复保存完整场景数据。`project.json` 只引用场景，不复制场景内容；`scenes/*.json` 是场景数据的唯一落点。

---

## 7. 默认项目参数

- 分辨率：1920 × 1080
- 帧率：30 fps
- 时间单位：所有持久化时间统一使用整数帧
- 项目尺寸与帧率必须保存在 `project.json` 中，不得散落在各场景

任何渲染、动画、时长换算都以这些参数为基准。

---

## 8. 编辑态与动画态

### 8.1 基础状态（Fabric.js 编辑的范畴）

Fabric.js 编辑的是图层的基础状态，即动画完成后的正常位置和样式。保存到 `scenes/*.json` 中时，这是图层的"最终落点"。

### 8.2 动画（Remotion 计算的范畴）

动画只保存相对于基础状态的变化（例如：从何处进入、旋转多少度、淡入淡出），由 Remotion 在播放和渲染时计算。

### 8.3 动效系统边界

- 采用 Keynote 式动效预设（例如：淡入、上滑、缩放进入等）
- 不开发 After Effects 式专业关键帧系统
- 不引入贝塞尔曲线编辑器、多轨关键帧、表达式等概念

---

## 9. 素材管理

- 素材路径使用项目相对路径或 `assetId`，禁止保存依赖本机目录的绝对路径
- Fabric.js 和 Remotion 必须使用相同的素材和字体，不得在两端各自维护不同的资源引用
- 字体、图片等一旦进入项目，统一由本地服务解析，编辑器与渲染器引用同一份资产描述

---

## 10. 保存策略（第一版）

第一版采用显式保存，不做自动保存与冲突合并。

具体要求：

- 保存前进行 Schema 校验，校验失败不得写入
- 尽量使用临时文件 + 原子替换的方式写入 JSON（例如：写到 `*.tmp` 再 `rename`）
- OpenCode 或 Pi 只修改已经保存到磁盘的数据，不直接改编辑器内存态
- 第一版不处理"编辑器未保存内容"与"外部 AI 修改"之间的自动合并，二者发生冲突时由用户决定保存或丢弃

---

## 11. 开发原则

- 每次只推进一个边界明确、能够运行和验证的步骤
- 不一次生成整个项目骨架
- 优先完成纵向可运行闭环（Vertical Slice），而不是横向铺开所有功能
- 避免过度抽象和过度工程化
- 不为尚不存在的未来需求设计复杂架构
- 未经明确确认，不得改变已经确定的核心技术架构

任何超出本文件范围的扩展（新的技术选型、新的领域模型字段、新的工作流阶段）都必须先回到本文件讨论并明确更新。

---

## 12. 第一阶段最小闭环

第一阶段唯一的目标是打通以下链路：

1. 打开一个本地示例项目
2. 读取一个场景
3. 在 Fabric.js 中编辑文字和矩形
4. 保存回领域模型 JSON
5. 由 Remotion Player 读取同一份数据，并显示基本一致的布局

完成该闭环后，再讨论进入下一阶段。在该闭环完成前，不引入 SRT 解析、不引入 AI 生成、不引入 Remotion 渲染输出、不引入 DaVinci Resolve 集成。

---

## 13. 范围外清单（再次明确）

以下能力在项目中不考虑，任何"顺手做一下"的提议都应被拒绝：

- 多用户
- 账号系统
- 云端数据库
- 多人协作
- 移动端
- 云端渲染
- 音频编辑
- 原始视频剪辑
- 人像和录屏轨道
- 通用视频编辑器能力
- 专业关键帧曲线系统
