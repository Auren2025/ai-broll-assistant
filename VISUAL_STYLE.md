# B-roll 视觉风格偏好

本文记录已确认的个人视觉偏好，供后续设计和修改 B-roll 场景时参考。

本文只描述风格原则与已完成作品的参考方法，不保存具体场景方案，也不替代 Project / Scene JSON。领域约束与工作边界以[AGENTS.md](AGENTS.md) 为准，阶段操作步骤见[项目调度 Skill](.agents/skills/ai-broll/SKILL.md)；文档导航见[README.md](README.md)。

## 核心目标

- 所有视频均为 faceless，口播由 B-roll 动画、产品录屏和有意留白共同承载，不假设存在真人镜头。
- 目标是简洁、规整、易读的 Keynote 式产品讲解：用较少元素、足够大的尺寸、准确的分组和舒服的出现顺序呈现信息，而不是增加设计元素来证明“完成度”。
- 即使忽略最终剪辑的背景、字幕和包装，前景也应清楚、有重心；但不要求每页同时具备图片、动作、标题和结论。
- 一个场景承载一个连贯视觉命题。解释可以长时间保持同一主画面；必要递进留在当前场景，不按每句话拆页或做动画。
- 优先级是：信息准确 → 表达合适 → 尺寸与阅读顺序 → 必要动画 → 少量视觉修饰。后面的手段不能弥补前面的缺失。

## 已确认的参考与使用边界

以下路径相对于工作区根目录，是本地已有作品的只读参考，不是需要复制或维护的模板。项目数据默认不纳入 Git；若参考不存在，应明确说明并使用本文原则，不创建替代项目。生成前按当前语义选择相关参考，不要求每次通读所有参考项目。

| 参考 | 借鉴内容 | 不应照搬的内容 |
| --- | --- | --- |
| `app/projects/my-design/scenes/scene-001.json` | 用户手工制作并明确喜欢的主要风格基准：大 Logo、规整分组、无填充的彩色描边容器、白色常规字重文字；先区域后内容，整组与分项入场按阅读需要选择 | 不把双栏、描边、具体尺寸、旧模型名称或 5 秒时长变成统一模板；该项目无有效字幕，不作为口播时序参考 |
| `app/projects/opencode/scenes/scene-004.json` | 真实 Session 截图作为主要讲解对象，旁边补充少量要点 | 不照搬全部标题、字号、色彩和逐项动画 |
| `app/projects/opencode/scenes/scene-recording-command-demo.json` | 用真实配置截图和指示箭头讲具体字段，避免重画一个伪界面 | 不复制历史文件名、坐标和素材路径到新项目 |
| `app/projects/opencode/scenes/scene-recording-agent-demo.json` 与 `app/projects/opencode/scenes/scene-010.json` | 同一配置截图从展示延续到解释，移位缩小让出讲解区域；稳定对象承接焦点变化 | 不把每页都做成“截图 + 多层卡片”，不默认跨场景必须移动 |

- `my-design` 用于审美校准；OpenCode 是用户认为已可接受但仍有差距的作品，只借鉴上述表达方法，不把全项目当成理想答案。
- 参考时检查实际素材、图层和动画；有预览条件时查看画面。只读 JSON 不等于已看过最终效果。
- 学习比例、信息组织和阅读节奏，不复制完整场景、旧文案或逐帧数据，不修改参考项目。跨项目需要使用的素材仍须经本地服务上传至目标项目。
- 用户确认某场景“可以了”只表示该场景可继续，不自动表示其所有细节成为长期偏好。新的长期偏好需用户明确确认后再更新本文。

## 按语义选择表达

先判断观众需要看懂什么，再选择最直接的承载方式：

| 内容 | 优先考虑 | 避免 |
| --- | --- | --- |
| 产品识别、回顾已有作品 | 真实 Logo、封面或产品主视觉，加必要名称 | 伪 Logo、重复产品名、额外宣传口号 |
| 分类、目录、模型名称、命令或路径清单 | 大文字、规整列表、网格或有意义的分组 | 为每项强配图标、用箭头把并列关系画成因果流程 |
| 具体界面、配置字段、操作证据 | 真实截图或其所需局部，配少量标注 | 用形状重画界面、让观众阅读缩小的整屏文字 |
| 原理、流程、包含、对比、状态变化 | 必要对象与关系的示意结构 | 只写出结论却没有表现要解释的关系 |
| 连续产品操作 | 交给实际录屏，B-roll 仅承担必要辅助 | 用逐页截图勉强模拟全部操作 |

这些是选择方法，不是固定模板，也不新增持久化分类字段。抽象对象可用文字和形状表示；不得把抽象示意图伪装成真实产品界面。

## 构图与前景完成度

- 确定第一落点和阅读顺序。焦点可以是 Logo、截图、文字列表、两组对比或结构本身，不强制增加一个独立的“主对象”。
- 元素要足够大，留白用来突出内容，不是把所有内容缩小。在常用缩小预览尺寸下，关键文字和需要讲解的截图区域仍应可读。
- 同类对象优先采用相同尺寸、整齐行列、统一间距和明确对齐。默认不旋转，不随机散落或交错堆叠。
- 避免非预期遮挡、越界与拥挤；必要的包含关系、截图标注或有意画面替换可以覆盖，但必须检查运动中的状态。
- 标题、底板、箭头和底部结论都不是必需品。移除后不影响身份识别、分组或理解的元素，应优先删除。
- 不机械重复“标题 + 多张卡片”，也不为了避免卡片而强行增加图片或流程图。规整的双栏文字与语义容器可以是完整设计。
- 标题不必默认顶部居中；Logo 已建立身份或文字结构已表达主题时，可以不再添加标题。

## 文字、容器与色彩

- 面向中文观众，以中文为主要画面语言；产品名、命令、代码和惯用英文术语保留英文。不添加文稿未表达的能力、分类、口号或结论。
- 文字可以是命名、关键词、分类、清单或必要结论，不照抄整段字幕。画面文字准确，不等于必须复述所有口播。
- 优先用位置、字号、间距和分组建立层级。常规字重可以作为默认；粗体只用于真正的重点，不把 800/900 字重铺满画面。
- 具体内容可以比分组说明更大。以 1920 × 1080 的 `my-design` 为比例参考：分组说明约 40px、列表内容约 50px；这是起点而非硬性数值，最终按实际内容和缩小预览调整。
- 不用连续空格排列多个标签。需要独立定位或独立出现的条目使用独立 Text；整体出现且排版一致的简单多行列表可以共用一个 Text。
- 不默认为每个标签添加底板。容器确实表示分类、包含或对比时，可使用无填充的圆角描边框，也可用克制填充；不叠加多层无意义底板和重复轮廓。
- 描边粗细按画面比例和可见性选择，不限制为极细线；也不要求每个容器都同时有填充和描边。
- 透明背景与黑色辅助预览底板上，文字默认以白色、灰色为主。颜色先用于必要分组、边界、对象身份或状态，不要求每个关键词染色。
- 品牌色来自真实素材；其余色彩少量、稳定地使用。同一概念跨场景尽量保持视觉身份。不把全项目限制为灰橙，也不为“丰富”凑齐多种颜色。

## Logo 与图片

- 当画面需要 Logo、图标、真实界面或具体插画时，使用真实 Image；缺少素材时使用 `src: null` 占位，不用字母、形状或手绘图标冒充。
- 纯名称清单、命令、路径和抽象分类不必逐项配图；只有当图像确实承担识别、展示或证据作用时才提出素材需求。不要求难以查找的泛化“概念图标组”。
- Logo 如需名称，保留简短文字；字标已经清晰表达产品名时不重复命名。Logo 与名称需要一起运动时使用 Group，不能因此引入嵌套 Group。
- 品牌图标占位默认圆角正方形；横向字标按真实品牌比例使用，不硬塞方框。截图按实际需要的区域比例设置。
- 素材在构图阶段参与决策：明确是整屏还是局部、必须可读的字段或区域、宽高比与透明底要求，以及素材自带文字是否已足够。
- 截图若需局部，应向用户说明所需裁取区域；不要假设编辑器支持额外裁剪字段。已上传素材先看真实内容再布局，不仅根据文件名猜测。
- 每个占位框配一个独立可见的中文 Text 素材说明，简短标出对象和关键要求；完整要求保留在 Image 的 `name` 中。它是制作导航，不是观众文案。
- 素材说明默认是紧邻 Image 上方的独立顶层 Text，不与 Image 组成 Group；若 Image 在 Group 内或包含动画，说明仍需在实际画布上同步，不能只机械复制局部坐标或动画数值。
- 替换素材时保持 Image 的位置、尺寸、变换和动画，删除对应素材说明。若真实素材与占位需求不符，先确认需要其他素材还是调整设计，不静默改布局。

## 动画与阅读节奏

- 静态是起点。新关键对象出现、必要关系或状态变化、流程推进和必要焦点转移才需要动画。为帮助理解而控制信息出现顺序也是有效目的，不仅限于演示物理运动。
- 先决定“开场有什么 → 新内容出现时旧内容怎么办 → 最后停在哪里”，再选择预设；解释段允许保持不变，不按字幕逐句触发。
- 品牌锚点或已知结构可以开场即存在，不必所有元素都入场。容器帮助建立阅读区域时，可以先容器后内容；容器与内容不必永远分开动画。
- 以阅读单元而非图层数量调度：同一组可整体出现，需要逐项讲解的内容再分步；通常一次推进一个主要阅读单元，不让多个无关区域同时争抢注意力。
- Build In 以 Fade and Move、Wipe 或纯淡入等简单方式为主，方向保持连贯、幅度克制。动画时长以可读和落定为准，不照搬参考的固定帧数。
- 不追求预设种类或 Action 数量。场景甚至整个项目主要使用简单 Build In 可以是正确结果；不能为了避免“都是入场”而强加 Magic Move、Scale Big 或 Bounce。
- 当语义确实依赖同步、回退、移动或状态改变时，应检查画面是否表现了这个关系，而不只是让一句描述它的文字入场。仅在更有助于理解时选择连接线揭示、Magic Move 或必要的显隐变化。
- Magic Move 用于持续移位、缩放让位或弱化以转移焦点，不做无意义脉冲。Build Out 仅用 Dissolve，按需使用；不默认全部退场，不用乱飞、旋转和弹跳填满时间。
- 同一视觉对象跨相邻场景继续被解释时，优先考虑保持身份、位置或从上一场结束状态自然过渡；只调整当前获批场景，不回改已完成场景，也不引入跨场景私有数据。HTML 每页独立播放仍需可理解。

## 视觉检查

- 数据校验与视觉检查分别通过；合法 JSON 不代表画面合格。实际检查方法由 Animator Skill 执行。
- 检查开场、关键变化前后、运动中间状态、最后停留和相邻场景衔接；确认焦点、可读性、素材标注位置与遮挡。
- 分别判断静态构图是否清楚，以及播放时能否按顺序理解。没有实际预览时明确标记视觉检查未完成，不用对 JSON 的推测代替观看结果。
- 最终交付不得包含未替换的关键占位框或可见制作说明；草稿必须明确标注并列出素材需求。不因视觉检查而自动渲染视频或导出 HTML。
