# 产品演进记忆

本文保存跨作品的改进想法、实验结果和已确认的非根本工作方法。它不是固定产品规格，也不替代 `AGENTS.md`、应用源码或具体作品数据。

写入任何条目前，先按照 `project-memory` Skill 向用户展示候选记忆并获得确认。只影响一个作品的内容应写入该作品的 `app/projects/<project-id>/MEMORY.md`，不要写在这里。

## 候选改进

尚未决定是否实施的跨作品想法。

目前没有记录。

## 当前工作方法

用户已经确认可以继续采用、但仍允许通过实践调整的方法。

目前没有记录。

## 实验与结果

记录尝试内容、适用范围和实际结果，避免只保存脱离背景的结论。

目前没有记录。

## 已实现改进

记录已经落地且需要后续观察的改进；实际行为仍以当前代码和 `app/README.md` 为准。

目前没有记录。

## 已否决或被替代

保留必要原因，避免以后无意重复同一条无效路线。

目前没有记录。
