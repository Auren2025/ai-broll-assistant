---
description: Review the current discussion and propose durable memory entries for confirmation
---

Load and follow the `project-memory` skill. Review the current conversation and propose only information worth carrying into a later session. Do not write any memory until the user confirms the numbered entries.

If `$ARGUMENTS` names a project ID, use it as the target creative project. Otherwise infer the project only when it is unambiguous; ask one short question when it is not.

Additional focus from the user: $ARGUMENTS
