# AI-Broll-Assistant

本项目是仅供个人使用的本地 YouTube B-roll 动画制作工具。

本文件只定义长期有效的边界、架构和数据原则。实现必须遵守本文件；改变这些原则前必须先获得用户确认。

## 1. 项目边界

项目负责：

- 从 SRT 拆分场景
- 生成结构化画面
- 手动编辑图层
- 预览和渲染动画
- 输出透明背景视频
- 输出手动翻页的交互式 HTML 演示
- 将结果交给 DaVinci Resolve

项目不负责：

- 原始视频剪辑
- 音频编辑
- 人像或录屏轨道
- 通用 NLE 能力
- 多用户和账号系统
- 云端数据库与云端渲染
- 多人协作
- 移动端
- 专业关键帧曲线系统
- 产品化与 SaaS

## 2. 核心工作流

```text
SRT
→ broll-scene-planner 生成语义场景方案
→ 用户确认场景边界、透明空隙和视觉意图
→ 写入空图层的领域 Scene JSON
→ broll-scene-animator 生成图层与动画
→ Fabric.js 手动编辑
→ Remotion 预览和渲染
├→ 透明背景视频 → DaVinci Resolve
└→ Remotion Player → 交互式 HTML 演示
```

AI 只生成和修改结构化领域数据，不生成一次性场景代码，也不直接操作编辑器内存。

## 3. 固定架构

- React：编辑器界面、内存状态、撤销与重做
- TypeScript：完整类型约束
- Fabric.js：图层基础状态与布局编辑
- Remotion：动画计算和视频渲染
- Remotion Player：实时预览，可打开独立预览窗口
- Node.js 本地服务：项目、场景、素材和渲染管理
- OpenCode / Pi：读取 SRT，生成和修改已落盘数据

不得用其他技术替换以上职责，也不得为同一职责引入第二套实现。

## 4. 数据原则

Project / Scene 领域模型 JSON 是唯一持久化数据源。

```text
OpenCode / Pi → Schema 校验后的领域 JSON
领域 JSON → 本地服务 → React / Fabric.js
领域 JSON → Remotion Adapter → Remotion
```

- React 只持有与领域模型一致的内存态
- Fabric.js 和 Remotion 不持有独立数据
- OpenCode / Pi 批量生成或调整项目数据时必须保持编辑器关闭，并直接修改目标项目的领域 JSON；编辑器内的交互修改统一通过本地服务
- 禁止持久化 Fabric Canvas JSON 或 Fabric Group 私有结构
- 禁止让 Remotion 读取 Fabric.js 私有数据
- 禁止重复保存完整场景
- 所有持久化数据必须通过严格 Schema 校验

## 5. 项目与场景

```text
projects/<projectId>/
├── source.srt
├── project.json
├── scenes/
├── assets/
├── audio/            (可选)
├── renders/
└── exports/          (可选)
```

- `project.json` 保存项目参数、场景引用和可选 `audioFile`
- `scenes/*.json` 保存完整场景数据
- `assets/` 保存项目素材
- `audio/`（可选）保存口播音频文件；`project.json` 的 `audioFile` 引用 `audio/<file>`，由本地服务提供，只在 Studio / 交互式预览中播放；透明视频默认静音渲染
- `renders/` 保存渲染结果
- `exports/html/`（可选）保存可离线打开的 HTML 演示；导出物不是领域数据源
- 项目至少包含一个场景
- 分辨率和帧率只保存在 `project.json`
- 所有时间使用整数帧

### 场景语义划分

- 从 SRT 生成项目时，必须先通读完整内容并理解叙事结构，再选择需要 B-roll 的段落和划分场景
- 场景边界由语义决定；字幕停顿、单条字幕边界、固定时长和字幕数量只能作为辅助信息，不得作为唯一划分依据
- 每个场景必须表达一个连贯的视觉意图；主题、论点、步骤、对比关系或叙事阶段发生变化时，必须重新判断是否结束当前场景或建立新场景
- 同一语义即使跨越多条字幕或包含停顿，也应保持在同一场景；没有明显停顿但语义已经变化时，也必须拆分场景
- 不是每个语义段落都必须生成场景；不需要 B-roll 的口播、真人画面和录屏段落应保留为透明空隙
- 场景 `topic` 必须概括该场景的语义和视觉目的，不得直接用首条字幕代替语义分析
- 自动切分工具只能生成候选时间骨架；OpenCode / Pi 必须审核并按语义合并、拆分或重建后，才能生成最终场景数据
- `npm run skeleton -- <projectId>` 只打印基于停顿的候选分组和时间锚点，不修改 `project.json` 或场景文件
- 新建项目或重新划分场景时，必须先在对话中展示完整场景方案；用户确认前不得写入或替换项目场景数据
- 未确认的场景方案只存在于当前对话，不得保存为另一套 Storyboard、Markdown 或 JSON 数据源
- 用户确认后，先将场景方案写入以 `layers: []` 表示的领域 Scene JSON，再基于同一文件生成图层与动画

### 场景时间与口播锚定

- 场景时间轴锚定口播：每个场景的 `startFrame` 是绝对时间锚点，对应 SRT 中该段口播的开始
- 首次由 SRT 生成时，场景必须按时间递增且不得重叠；开头、场景之间和结尾都允许存在不需要 B-roll 的透明空隙
- 手动编辑场景内容（图层、动画）不影响任何时间锚点
- 手动编辑场景时长时，时长被钳制到下一场景的 `startFrame` 之前，不允许重叠；缩短时长允许产生空隙（空隙期间口播单独播放）
- 场景引用顺序必须与 `startFrame` 递增顺序一致
- 删除场景不自动平移后续场景（保持口播锚点）；由此产生的空隙是允许的
- 重叠属于数据错误，必须校验拒绝；空隙允许但校验时给出提示
- 校验工具必须检查以上时间约束

默认参数为 1920 × 1080、30 fps、透明背景。

编辑画布和 Remotion 交互式预览对没有 `backgroundColor` 的场景显示黑色辅助底板；该底板不是领域数据，Scene / Project Composition 与最终渲染仍保持透明。显式场景背景色在预览和渲染中都生效。

## 6. 图层模型

支持以下图层：

- Text
- Image
- Rectangle
- Circle
- Triangle
- Arrow
- Group

Image 可以使用 `src: null` 表示尚未载入素材的灰色图片占位框；载入后仅引用项目 `assets/` 目录内已经存在并通过校验的素材，且必须经过本地服务上传。占位框载入或替换素材时保持图层位置、尺寸、变换和动画不变。
Image 使用 `fit` 控制素材适配：`contain` 保持比例完整显示，`fill` 拉伸填满；新建占位框默认使用 `contain`，旧数据缺少该字段时按 `fill` 处理。
Image 支持统一四角圆角与可选描边，其他视觉属性复用基础变换与透明度。
Rectangle 与完整 Circle / Ellipse 支持 `shapeText` 内嵌文字；Circle 仅在 `donut = 0` 且 `sweep = 360` 时允许显示和编辑内文，圆环与不完整扇形不支持有效内文。新建 Rectangle / Circle 必须显式带默认空文字配置，旧数据缺少该字段时按默认空配置读取。
形状内文属于形状自身，不是独立 Layer：没有 ID、坐标、层级、变换、可见性、锁定或动画。双击形状直接编辑内文，但选择仍使用形状图层 ID；内文只跟随形状的移动、缩放、旋转、透明度和动画，不应用第二套动画，也不出现在图层树或时间线中。
形状宽高固定；内文在形状内边距定义的固定内容框中自动换行并裁剪溢出。Fabric.js 与 Remotion 必须尽可能保持相同的文字样式、换行、对齐和裁剪结果。现有 Group + Text 不自动迁移为形状内文。
卡片由文字、图片、图形和 Group 组合，不作为独立图层。

### 变换

- `x`、`y` 是图层顶点的左上角
- `width`、`height` 是图层在画布上的视觉尺寸，是尺寸的唯一来源
- `rotation` 是围绕图层中心的旋转
- 动画和适配器只能读取 `width`、`height`，不允许引入额外的缩放字段
- Fabric.js 与 Remotion 必须使用相同的坐标和变换语义

Inspector 直接编辑 `width`、`height`，Fabric.js 拖拽结束后把视觉尺寸写回 `width`、`height` 并把内部 transform 重置为单位缩放。任何操作都不会产生"基础尺寸 + 缩放比例"的二义性。

### Group

- Group 子图层使用 Group 局部坐标，子图层的 `x`、`y` 是相对 Group 左上角的偏移
- Group 子图层只能是普通图层
- 不支持嵌套 Group
- Group 自由拉伸宽高时，子图层按各自比例同步缩放并保持相对位置（Figma 风格）
- Group 不具有 Fill、Stroke、圆角或裁剪
- Group 和子图层分别保留样式、变换与动画
- Group 子图层必须能够选择、编辑、对齐和配置动画
- 创建或取消 Group 不得改变视觉结果
- 无法无损取消的 Group 不得被静默展开

### Text

Text 支持三种尺寸模式：

- `both`：自动调整宽度和高度
- `height`：固定宽度，自动调整高度
- `fixed`：固定宽度和高度

文字内容或排版属性变化时按对应模式重新测量。Fabric.js 与 Remotion 的换行、溢出和垂直对齐必须一致。

## 7. 编辑与动画

Fabric.js 编辑图层的基础状态；Remotion 根据基础状态计算动画。

动画只保存相对于基础状态的变化，不保存逐帧结果。

- 动画使用场景局部整数帧
- 动画区间为 `[startFrame, startFrame + durationInFrames)`
- 动画时长至少为 1 帧
- 动画最后一帧必须达到预设最终状态
- 动画不得超出场景时长
- 每个图层最多包含一个 Enter、一个 Emphasis 和一个 Exit 动画
- Group 动画与子图层动画按父子层级组合
- 时间线只编辑领域模型，不保存独立数据

动效采用 Keynote 式预设，不引入自由关键帧、曲线编辑器、表达式或多轨动画系统。

Fabric.js 与 Remotion 对同一份数据必须保持视觉一致。

### HTML 演示播放

- 每个场景对应一页，页面顺序与 `project.json` 的场景引用顺序一致
- 进入某页时从场景局部第 0 帧自动播放该页全部动画，播放完停留在最终画面
- 翻到上一页或下一页时，新页面从头播放；当前页必须能够手动重播
- 鼠标点击、空格键和左右方向键只负责翻页或导航，不为单个动画引入点击步骤
- HTML 演示必须复用领域模型与 Remotion 动画计算，不保存独立场景或逐帧结果
- 导出物必须包含运行所需的场景数据和项目素材，打开后不得依赖本地服务

## 8. 素材原则

- 素材使用项目相对路径或 `assetId`
- 禁止保存本机绝对路径
- 所有素材必须位于项目目录内
- Fabric.js 与 Remotion 使用相同的图片和字体
- 素材统一由本地服务解析
- 缺失素材必须明确报错

## 9. 自动保存与同步

编辑器采用自动保存，不要求用户手动保存。

- 修改经过短暂合并后自动写入
- 只保存发生变化的文件
- 保存请求串行执行
- 保存前必须通过 Schema 校验
- JSON 使用临时文件和原子替换写入
- 界面显示正在保存、已保存、失败或冲突
- 保存失败时保留内存内容并允许重试
- 撤销和重做同样自动保存
- 不得静默覆盖 OpenCode / Pi 或其他程序的外部修改
- 没有待保存修改时自动读取外部变化
- 存在本地修改时暂停写入，由用户选择保留磁盘内容或编辑器内容
- 涉及多个文件的操作不得产生部分保存或无效引用

## 10. 本地服务原则

- 只监听本机地址
- 只允许明确的编辑器来源调用写入和删除接口
- 禁止任意网页跨域修改本地项目
- 所有路径必须限制在项目目录内
- 所有输入必须校验
- 缺失或损坏的数据必须明确报错
- 文件操作失败不得伪装成成功

## 11. 运行与维护原则

当前默认工作是处理 `projects/<projectId>/` 中的实际 B-roll 项目，不是继续开发工具本身。

### 项目运行

- 只修改用户指定的目标项目；不得因生成场景而修改应用源码、Schema、Adapter 或构建配置
- 新项目必须先验证并通读完整 `source.srt`，再制定语义场景计划和写入领域数据
- 新建或重新划分使用 `broll-scene-planner`；确认后的场景生成与视觉修改使用 `broll-scene-animator`；跨阶段任务由 `ai-broll` 调度
- 已存在的项目必须先读取当前 `project.json`、场景和素材；保留有效 ID、人工布局、动画和时间锚点
- 继续处理或局部修改项目时，只改动任务涉及的文件，不得重新生成无关场景
- OpenCode / Pi 批量修改项目前必须确认编辑器未在写入；完成后通过严格 Schema 校验，再由用户打开编辑器检查
- 从 SRT 新建或重新划分场景后运行 `npm run validate:srt -- projects/<projectId>/source.srt`
- 每次项目数据修改后运行 `npm run validate:project -- projects/<projectId>`；透明空隙是正常结果，不使用连续覆盖校验作为日常要求
- 只有用户明确要求时才渲染视频或导出 HTML；生成物不是领域数据源
- 视频渲染前必须通过项目校验和本地服务健康检查；输出固定为带 Alpha 的 ProRes 4444，并默认不包含项目口播音轨

### 工具维护

- 只有用户明确要求修改工具能力或发现运行阻塞缺陷时，才修改应用源码
- 每次只解决一个边界明确的问题，不修改无关代码，不为未确认的需求设计架构
- 先确定领域语义，再修改 Schema、UI 和 Adapter；不通过放宽 Schema 掩盖实现错误
- 新增技术、图层类型、领域字段或工作流前必须先更新本文件并获得用户确认
- 源码修改必须通过相关 Schema 校验、Lint、构建和聚焦测试；纯项目数据修改不要求运行应用构建
